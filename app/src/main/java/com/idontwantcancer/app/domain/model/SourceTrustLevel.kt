package com.idontwantcancer.app.domain.model

/**
 * Defines the inherent trust level assigned to an intelligence source based on its 
 * historical record, transparency, and institutional authority.
 */
enum class SourceTrustLevel {
    VERY_LOW,
    LOW,
    MODERATE,
    HIGH,
    VERY_HIGH
}
